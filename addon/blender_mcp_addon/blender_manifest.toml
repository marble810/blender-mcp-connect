schema_version = "1.0.0"

id = "blender_mcp_connect"
version = "0.1.0"
name = "MCP Connect"
tagline = "MCP server add-on for LLM interaction (unofficial downstream of Blender Lab MCP)"
maintainer = "marble810"
type = "add-on"
website = "https://github.com/marble810/blender-mcp-connect"
blender_version_min = "5.1.0"

license = [
  "SPDX:GPL-3.0-or-later",
]

tags = ["Development"]

[permissions]
network = "Runs a local TCP socket server for MCP client communication"
files = "Writes a per-instance runtime descriptor for automatic MCP client discovery"
